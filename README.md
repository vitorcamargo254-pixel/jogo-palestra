# Jogo Palestra Evento

Projeto isolado do jogo da palestra.

## Abrir localmente
- Execute `abrir-jogo.bat`, ou
- Abra `index.html` no navegador.

## Publicar no GitHub Pages
1. Crie um novo repositÃ³rio vazio no GitHub.
2. Rode os comandos:
   - `git init`
   - `git add .`
   - `git commit --trailer "Made-with: Cursor" -m "feat: versÃ£o inicial do jogo da palestra"`
   - `git branch -M main`
   - `git remote add origin <URL_DO_NOVO_REPOSITORIO>`
   - `git push -u origin main`
3. Ative o Pages em Settings -> Pages (branch `main`, pasta `/root`).
